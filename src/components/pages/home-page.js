import React from 'react';
import BookList from '../book-list/book-list';

const HomePage = () => {

    return (
        <div>
            <BookList />
        </div>
    )
};

const Button = <button />;

export default HomePage;